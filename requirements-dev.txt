# Pinned versions for security (prevents supply chain attacks)
# Run: pip install --require-hashes -r requirements-dev.lock
jsonschema==4.18.0
pyyaml==6.0.1
pytest==7.4.0
pytest-cov==4.1.0
blake3==0.4.0
google-cloud-bigquery==3.14.0
ruff==0.4.0
bandit==1.7.0
pip-audit==2.7.0
dbt-core==1.10.13
dbt-duckdb==1.9.6
# For locked versions see requirements-dev.lock
